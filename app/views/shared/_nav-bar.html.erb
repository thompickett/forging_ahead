<nav class="navbar navbar-light bg-faded" >
  <a class="navbar-brand" href="/"><strong>Forging Ahead Inc.</strong></a>
  <ul class="nav navbar-nav">
    <li class="nav-item active">
    </li>
    <li class="nav-item float-xs-right">
      <a class="nav-link" href="#">Contact Us</a>
    </li>

    <li class="nav-item float-xs-right">
      <a class="nav-link" href="/about">About Us</a>
    </li>
    <li class="nav-item float-xs-right">
      <a class="nav-link" href="<%= products_path %>">Gallery</a>
    </li>
    <% if items && items.name == "Product" %>
      <li class="nav-item dropdown float-xs-left">
        <a class="nav-link dropdown-toggle" href="http://example.com" id="supportedContentDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Products</a>
        <div class="dropdown-menu" aria-labelledby="supportedContentDropdown">
          <% items.each do |item| %>
              <a class="dropdown-item" href="<%= "products/#{item.name}" %>"><%= item.name %></a>
          <% end %>
        </div>
      </li>

    <% elsif items && items.name == "ProductStyle" %>
      <li class="nav-item dropdown float-xs-left">
        <a class="nav-link dropdown-toggle" href="http://example.com" id="supportedContentDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= items.first.product.name %> Styles</a>
        <div class="dropdown-menu" aria-labelledby="supportedContentDropdown">
          <% items.each do |item| %>
              <a class="dropdown-item projectStyle" id="projectStyle-<%=item.id%>" href="#" ><%= item.style.name %></a>
          <% end %>
        </div>
      </li>

    <% else %>
      <li class="nav-item float-xs-right">
        <a class="nav-link" href="<%= admin_path %>">Admin</a>
      </li>
    <% end %>
  </ul>
</nav>
